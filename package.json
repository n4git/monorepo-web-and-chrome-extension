{
  "name": "monorepo-web-and-chrome-extension",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev": "turbo dev --filter=web",
    "build": "turbo build --filter=web",
    "lint": "turbo lint --filter=web"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
